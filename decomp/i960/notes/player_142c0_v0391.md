# v0391 — player 0x142c0 geometry-expansion body (0x142c0->0x14310, 56/+3/+3)

## Boundary

`hybrid_execute_player_142c0` is the accepted native-admitted
geometry-expansion body following the `0x1428c` head. It is chained
directly in `hybrid_first_dispatch_task_execute` (`case 0x142c0` at
line 25705, fall-back interpreted via `hybrid_execute_interpreted_task`).
No focused ROM-backed fixture exists yet (open); the boundary is
evidenced by:

- `native-sixth-dispatch`: MATCH (6 repeated tasks through full chain,
  870 blocks, 7,404,901 instructions; the player body is one of the
  seven first-dispatch tasks).
- `vf2i960 resume-trace rom-dir at142c0.vf2snap --until 0x14310`
  → 56 steps / +3 calls / +3 returns from `0x142c0`. Callee `0x4b838`
  (command setup + 5-slot copy + ret) consumes the bulk; tail
  `0x142c4→0x142e4` is the `call 0x4b5d0` + return (compare/state).
- Existing C (`hybrid_execute_player_142c0`): gates `ip==0x142c0`,
  `g1<=1`, non-degenerate `table_entry` (`player+0x1a0` via
  `0x0050083c`; phase `0x530005` must be `0`); writes command
  `0x550000` family (`+0x10` per `g1` index) with `g0/g1/g2`, sets
  `bit 21` in player_flags, exits `0x14310` (56-insn count
  `g1==1 ? 55 : 56`; 3 nested calls / 3 returns from the body).

## Witness (partial, probe)

From `at142c0-a.vf2snap` (restored from `pre14288.vf2snap` through
`0x142c0`):

- `ip == 0x142c0`, `g1 == 1`, `g0 == 0xaf` (`frame_selector`),
  `g2 == 0x2c78ae0` (`table_pointer`), `player_flags == 0x04000800`
  (bit 11 from corridor + bit 21 set by body), `record_pointer ==
  0x0201c2fc`, `scratch_base == 0x00520000`, `phase == 0` (`0x530005`).
- Memory: `0x550000 = 1` (command set); `0x5502c0` family = command;
  scratch slots populated by the head's wrapper (already verified
  in v0390); player `+0x10`/`+0x0c` = `0x501500` / `0x142f4`
  (tail stores); F0 stays `0x04000800`.

A full byte-exact slot pin against the `0x4b838` callee (five-slot
geometry command outputs, 36-word float-conversion window,
`cvtri`/`stos` triple, cursor rewinds) requires a dedicated
five-slot reference trace comparable to `player_270d4_slot_pin_v0358.md`.
That is the remaining open work for this boundary.

## Scope

- `hybrid_execute_player_142c0` accepts `g1 == 0` (single command) and
  `g1 == 1` (two commands at `+0x10`/`+0x10+0x10`); `g1 > 1` stays
  unsupported (unmeasured).
- `hybrid_execute_player_4b5d0` (sub-helper) covers the measured
  `table_index` lookup (`player+0x1b1`), `0x020062a0` ROM table,
  scratch stores at `+0x76c` family. Degenerate (`table_entry == 0`)
  exits without storing; other siblings fail-closed.
- The `0x550000` command family uses absolute addresses from the ROM
  (`0x005502c0` / `0x005502d0` / `0x005502b0` / `0x005502a8`); only
  `g0 == 0xaf` with measured `frame_selector` produces the exact
  command and payload observed from the snapshot.

## Validation

- `native-sixth-dispatch`: MATCH (confirms the body runs correctly as
  part of the full first sweep).
- `hybrid_execute_player_142c0` reaches `0x14310` with correct
  register/memory poststate when driven from `at142c0-a.vf2snap`
  through `hybrid_first_dispatch_task_execute` (chain: 142c0 body
  → 14310 setup → 143e4 prefix → ... → scheduler return).
- No focused differential fixture (`test_player_142c0_*.c`) yet; the
  body is proven through the full-dispatch chain and the partial
  `resume-trace` witness above. Adding a dedicated fixture with byte-
  exact `0x4b838` output comparison is the remaining open evidence step.

## Status

Native-admitted (`hybrid_execute_player_142c0`). Next verification
step: a focused ROM-backed `0x142c0` fixture comparing the five-slot
geometry-output window (`0x550000` family) and float-conversion triple
at `0x217d2ac`-region against a `resume-trace` reference, matching
`player_270d4_slot_pin_v0358.md` depth. Once that pins, the complete
`142c0 → 14310 → 143e4 → 1abf4 → ... → 29414` chain is fully covered.
