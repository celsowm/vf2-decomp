# v0358 — coli `0x22404` live body 77 (medição; C ainda não estendido)

## Witness medido (v0352, confirmado na fronteira)

Span live primeiro `0x22404`: **78** passos até `ret 0x225b0`
(body **77** + ret). Gates no momento do query:

| Campo | Valor |
| --- | --- |
| `f0+0x1a4` | `0x00010000` (bit16) |
| `f0+0x820` | `1` |
| `f0+0x5b8` | `0` |
| `f0+0x1aa` / `+0x808` | `0` / `0` |
| slot | `0` |
| exclude | `0xffff` |
| ROM mask | `0x02007ace` (index via `0x02007aca+idx*4`) |
| FIFO | `0x884000` |
| stores | `f1+0x6d4`, `+0x65c..+0x664` |
| `g0` final | `1` |

Diferente do shape unit v0303 (body **72**, one-hit scan). C
`coli_22404_body` cobre warm 13 / sibling 29 / unit 72; **não** o live 77.

## Status v0358

Não estendemos `coli_22404_body` nesta fatia: o delta body72→body77 exige
trace de stores/FIFO por insn no park live, e o master está em gate de
não-quebrar MATCH (Fase A bloqueada em COBR CC). Fail-closed permanece.

## Status v0382 (closed)

Steps 1–3 done: live-77 recovered (`stale → bal 0x225bc +5`,
prologue 22/27; stale+empty and stale-slot-1 fail-closed; final
CC=EQUAL pinned on the proven path) with unit + ROM-backed
`vf2_coli_22404_live` fixture, plus a `bo`/`bno`-after-scanbit oracle
fix in the arch wrapper. Step 4 (whole-tail composition) deferred with
measured triple. See `fa_coli_22404_live_v0382.md`.

## Reabertura

1. `vf2probe` no park live (`coli-22404-e1` / midbody recipe) com
   `--memory-trace` filtrando `f1+0x6d4` e FIFO `0x884000`.
2. Isolar o delta de corpo 72→77 (stores extras / ramo de scan).
3. Extensão mínima em `coli_22404_body` só com gates medidos; unit
   13/29/72 continuam; novo unit 77 + negative.
4. Só depois pin whole-task midbody 380 / 9393.

Parks locais: `out/coli-22404-*.vf2snap`, `out/coli-live-midbody-g01.*`.
