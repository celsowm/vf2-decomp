# `fa_game_info` positive mask `0x16` through threshold 9

The state-8 positive composition `0x00000016` (bits 1+2+4) is admitted in the
existing measured mixed-low corridor through threshold `9`.

The controlled `0x164ac` matrix covers the three measured fighter-record
distributions, both countdown values, both mode-bit-6 values, and thresholds
`0` through `9`: `120/120` exact against the reference, including CPU,
condition, frame, procedure, counters, and mutable Model 2A state. The
threshold-10 control remains `0/12` native at `0x0001645c`.

No unmeasured distribution or new hardware/object-field semantics are admitted.
