# Positive masks `0x20` and `0x80` through threshold 3

The isolated state-8 positive compositions `0x00000020` (bit 5) and
`0x00000080` (bit 7) are admitted through threshold `3` using their uniform
two-instruction dispatcher correction.

The controlled `0x164ac` matrices cover the three measured fighter-record
distributions, both countdown values, both mode-bit-6 values, and thresholds
`0` through `3`: `96/96` exact against the reference, including CPU,
condition, frame, procedure, counters, and mutable Model 2A state. The
threshold-4 controls remain fail-closed (`0/12` native for each mask).

No unmeasured distribution or new hardware/object-field semantics are admitted.
